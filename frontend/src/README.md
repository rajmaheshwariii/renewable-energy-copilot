# Frontend source

- `App.jsx` — main dashboard screens and user interactions.
- `services/backend_api.js` — all requests sent to the FastAPI backend.
- `testing/demo_forecast_helpers.js` — deterministic demo helpers used only by automated tests.
- `App.css` / `index.css` — dashboard styling.
